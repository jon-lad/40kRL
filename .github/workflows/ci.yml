name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: windows-latest

    strategy:
      matrix:
        configuration: [Debug, Release]
        platform: [x64]

    env:
      VCPKG_DEFAULT_TRIPLET: x64-windows

    steps:
      - uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Bootstrap vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git C:\vcpkg
          C:\vcpkg\bootstrap-vcpkg.bat

      - name: Install dependencies via vcpkg
        run: |
          C:\vcpkg\vcpkg.exe install libtcod:x64-windows sol2:x64-windows lua:x64-windows --overlay-ports=${{ github.workspace }}/vcpkg-overlay/ports

      - name: Integrate vcpkg
        run: C:\vcpkg\vcpkg.exe integrate install

      - name: Build game
        run: msbuild 40kRL.sln /p:Configuration=${{ matrix.configuration }} /p:Platform=${{ matrix.platform }}

      - name: Verify SDL3 runtime
        run: |
          $outDir = "${{ matrix.platform }}\${{ matrix.configuration }}"
          if (-not (Test-Path "$outDir\SDL3.dll")) {
            Write-Error "SDL3.dll not found in $outDir"
            exit 1
          }

      - name: Build tests
        run: msbuild Tests\40kRL_Tests.vcxproj /p:Configuration=${{ matrix.configuration }} /p:Platform=${{ matrix.platform }}

      - name: Copy runtime DLLs to test output
        run: |
          $outDir = "${{ matrix.platform }}\${{ matrix.configuration }}"
          $vcpkgBin = "C:\vcpkg\installed\x64-windows\bin"
          $vcpkgDbgBin = "C:\vcpkg\installed\x64-windows\debug\bin"
          if ("${{ matrix.configuration }}" -eq "Debug") {
            Copy-Item "$vcpkgDbgBin\*.dll" "$outDir\" -Force -ErrorAction SilentlyContinue
          }
          Copy-Item "$vcpkgBin\*.dll" "$outDir\" -Force -ErrorAction SilentlyContinue

      - name: Run tests
        env:
          SDL_VIDEODRIVER: dummy
        run: |
          $testExe = "${{ matrix.platform }}\${{ matrix.configuration }}\40kRL_Tests.exe"
          if (Test-Path $testExe) {
            & $testExe --reporter compact
          } else {
            Write-Error "Test executable not found at $testExe"
            exit 1
          }
